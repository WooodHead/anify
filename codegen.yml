overwrite: true
schema: 'http://localhost:3000/api/graphql'
generates:
  typings/graphql/types.d.ts:
    plugins:
      - 'typescript'
      - 'typescript-operations'
    config:
      noExport: true
      avoidOptionals: true
